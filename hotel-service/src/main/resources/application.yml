server:
  port: 3002

spring:
  application:
    name: hotel-service
  datasource:
    url: jdbc:mysql://localhost:3306/userhotelrating
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true